services:
  postgres:
    image: postgres:17-alpine
    container_name: vladify-admin-api-postgres
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: ${DbName}
      POSTGRES_USER: ${DbUser}
      POSTGRES_PASSWORD: ${DbPassword}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DbUser} -d ${DbName}"]
      interval: 10s
      timeout: 5s
      retries: 5

  api:
    build:
      context: .
      dockerfile: Vladify/Dockerfile
    container_name: vladify-admin-api
    env_file:
      - .env
    ports:
      - "8080:8080"
    environment:
      ConnectionStrings__DefaultConnection: "Host=postgres;Port=5432;Database=${DbName};Username=${DbUser};Password=${DbPassword}"
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  postgres-data:
